#include <stdio.h>
#include <math.h>

int main() {
//        double a=4, b=2, c;
//        c = pow(b,a);
//        printf("%g^%g=%g\n", a,b,c);
//        return 0;
double base, exponent, power;

    base = 9.0;
    exponent = 2.0;
    power = pow(base, exponent);

    printf("%g ^ %g = %g\n", base, exponent, power);

return 0; 
}
